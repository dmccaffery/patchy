# Copyright 2026 Bitwise Media Group Ltd.
# SPDX-License-Identifier: MIT

# Triage for the lint-docker grype gate (.mise/hack/lint-docker.sh), which
# scans the external base images named in the Dockerfiles' FROM lines.
#
# Empty at present: the agent image dropped node/npm for the native claude
# binary, which removed the only vulnerable-package surface the gate ever
# flagged (npm's vendored node_modules). When a base-image finding must be
# accepted, add an ignore rule here pinned to the vulnerable version and its
# path — so a reintroduction anywhere else still fails the gate — with a
# comment saying why it is acceptable and when to delete it.
ignore: []
